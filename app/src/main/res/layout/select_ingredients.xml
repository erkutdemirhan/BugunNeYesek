<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <AutoCompleteTextView
        android:id="@+id/select_ingredients_autocomplete"
        android:layout_width="250sp"
        android:layout_height="wrap_content"
        android:completionThreshold="1"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        style="@style/AutoCompleteTextViewTheme" />

    <Button
        android:id="@+id/select_ingredients_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/select_ingredients_button"
        android:layout_alignParentTop="true"
        android:layout_toRightOf="@+id/select_ingredients_autocomplete"
        style="@style/ButtonTheme" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/select_ingredients_recyclerview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical"
        android:layout_below="@+id/select_ingredients_button" />

</RelativeLayout>