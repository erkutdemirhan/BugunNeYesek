<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erkutdemirhan.bugunneyesek" >

    <application
        android:name="com.erkutdemirhan.bugunneyesek.application.BugunNeYesek"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.SelectIngredientsActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action   android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name = ".activity.RecipeActivity">
            <meta-data
                android:name  = "android.support.PARENT_ACTIVITY"
                android:value = ".activity.RecipeListActivity" />
        </activity>
        <activity
            android:name = ".activity.RecipeListActivity">
        </activity>
    </application>

</manifest>
